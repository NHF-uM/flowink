融合http和new_http的网页代码：
    整体美术风格：采用http的颜色（蓝色）和风格；
    功能上：
        按键：选择图片按键、上传图片按键、两个带彩色的转换算法按键（去除黑白转换的两个按键）
        像素转换流程：浏览器得到任意格式和尺寸的图片——根据偏移值裁剪出800x480（当前功能已经满足条件）——转换为24位rgb888（1152054字节，满足flowink\flowink\server\svc_bmp.c的转换要求）——post上传（满足flowink\flowink\component\net\http_server.c接收流程）
        为了让用户能更好的选择呈现出来的效果，增加多个调节滑块（A算法不能调节B算法的参数，做一下限制）
            在误差扩散算法之下：
                权重缩放系数，范围 `0~2.0`，默认 1.0m，例如：errArr[bInd][i-1]=addVal(errArr[bInd][i-1],r,g,b,3.0 * errScale);
            在阈值算法下：
                阈值偏移
            二者都有：
                亮度偏移
                对比度偏移
        保留new_http的原图和结果效果图的展示功能，我有个想法，根据偏移值在原图上层覆盖一个细边框，能直观展示出裁剪效果？？
        仍然需要预留位置展示zkk.png；
        删除DEVICE ID的填写框，不需要填写，对应功能也可以删减
        new_http在上传的图片和转换的图片不会适应浏览器窗口大小，而且会跳出并覆盖底层背景的框架，很丑很丑，这个可以参考一下http的实现
        视觉上需要看起来能够清晰图片的边界，http在上传图片后能完整填充到蓝色边界内部，看起来很舒服（有一些图片是纯白的，new_http里面都分不清图片的具体边界）
    美术优化：
    节省资源：
        是否能够融合js文件（缩减文件数量），一次性全部发送让浏览器GET
        需要留意所有文件的代码体积总和，无用功能的相关代码要删掉，引入新功能时顺便给出体积改变预估值
        暂时不需要压缩空格和换行，我自己最后会做的

要求：
1. flowink\flowink\resources\http路径下的文件是经过验证，稳定可用的，上传下发的报文格式，**通讯流程**对齐该版本，只能修改new_http文件夹内文件（raw保留不动）！
2. 优先保证原有功能稳定，尽量不引入新问题、新BUG，如果引入了并且需要验证，在完成融合后要告知我
3. 开发工作必须遵循 先讨论方案、确认可行性，再落地代码修改 的流程，禁止直接开发、边写边改。重点解决两类核心问题：
   - 功能冲突问题：针对项目内两个文件夹存在的功能重叠、逻辑冲突，必须在方案讨论阶段完成梳理、取舍、兼容方案敲定，未解决冲突禁止进入开发阶段。
   - 需求模糊与新功能问题：针对原有需求文档描述不清晰、定义模糊的内容，必须提前沟通确认；针对拟新增的功能，优先核验技术可行性、体验优化价值，确认可落地、有正向优化效果后，再纳入开发范围。
4. 严格按照 功能板块为最小迭代单元 拆分开发进度，逐个需求、逐个功能落地实现，禁止一次性批量开发、全量改动，保障迭代可随时退回、局部调整。
5. 固定迭代优先级顺序，逐层落地：核心功能实现 → 美术样式适配 → 资源占用优化。优先保证功能可用、逻辑正常，再处理视觉展示，最后做性能、资源精简优化。
6. 所有前期方案讨论、冲突解决方案、需求补充定义、功能实现规则、优化调整要点，全部实时更新至本文档，作为后续人工迭代、AI理解、开发落地、版本维护的唯一标准依据，保证文档与实际开发逻辑完全一致。