var srcImg=null;var dX=0,dY=0;var curPal=[[0,0,0],[255,255,255],[255,255,0],[255,0,0],[0,0,255],[0,255,0]];var curAlgo=null;var dW=800,dH=480;function getElm(n){return document.getElementById(n);}function processFiles(files){var file=files[0];if(!file)return;var reader=new FileReader();reader.onload=function(e){var img=new Image();img.onload=function(){srcImg=toLandscape(img);drawSrc();};img.src=e.target.result;};reader.readAsDataURL(file);}function drop(e){e.stopPropagation();e.preventDefault();processFiles(e.dataTransfer.files);}function ignoreDrag(e){e.stopPropagation();e.preventDefault();}function toLandscape(img){if(img.height<=img.width)return img;var rot=document.createElement('canvas');rot.width=img.height;rot.height=img.width;var rctx=rot.getContext('2d');rctx.translate(rot.width,0);rctx.rotate(Math.PI/2);rctx.drawImage(img,0,0);return rot;}function drawSrc(){var src=getElm('source');src.width=srcImg.width;src.height=srcImg.height;src.style.display='block';src.getContext('2d').drawImage(srcImg,0,0);getElm('srcBox').innerHTML='';drawCropBox();}function drawCropBox(){var src=getElm('source');if(!src.width||!srcImg)return;src.getContext('2d').drawImage(srcImg,0,0);var kx=src.width/srcImg.width;var ky=src.height/srcImg.height;var ctx=src.getContext('2d');ctx.strokeStyle='#ff0000';ctx.lineWidth=Math.max(1,Math.min(kx,ky)*2);ctx.strokeRect(clampX()*kx,clampY()*ky,dW*kx,dH*ky);}function clampX(){return Math.max(0,Math.min(dX,srcImg.width-dW));}function clampY(){return Math.max(0,Math.min(dY,srcImg.height-dH));}function preProcess(p){var bright=parseFloat(getElm('val_bright').value)||0;var cont=parseFloat(getElm('val_cont').value);if(!(cont>0))cont=1;if(bright===0&&cont===1)return;var n=p.data.length;for(var i=0;i<n;i+=4){p.data[i]=Math.max(0,Math.min(255,(p.data[i]-128)*cont+128+bright));p.data[i+1]=Math.max(0,Math.min(255,(p.data[i+1]-128)*cont+128+bright));p.data[i+2]=Math.max(0,Math.min(255,(p.data[i+2]-128)*cont+128+bright));}}function getErr(r,g,b,c){r-=c[0];g-=c[1];b-=c[2];return r*r+g*g+b*b;}function getNear(r,g,b){var monoScale=1;if(curAlgo==='lvl')monoScale=1-(parseFloat(getElm('val_mono').value)||0)/100*0.5;var ind=0;var err=getErr(r,g,b,curPal[0])*(ind<2?monoScale:1);for(var i=1;i<curPal.length;i++){var e=getErr(r,g,b,curPal[i])*(i<2?monoScale:1);if(e<err){err=e;ind=i;}}return ind;}function setVal(p,i,c){p.data[i]=curPal[c][0];p.data[i+1]=curPal[c][1];p.data[i+2]=curPal[c][2];p.data[i+3]=255;}function addVal(c,r,g,b,k){return [c[0]+(r*k)/32,c[1]+(g*k)/32,c[2]+(b*k)/32];}function procImg(algo){if(!srcImg){alert('First select image');return;}curAlgo=algo;getElm('dstBox').innerHTML='<canvas id="canvas"></canvas>';var canvas=getElm('canvas');canvas.width=dW;canvas.height=dH;dX=Math.max(0,parseInt(getElm('nud_x').value)||0);dY=Math.max(0,parseInt(getElm('nud_y').value)||0);dX=clampX();dY=clampY();var src=getElm('source');var sW=src.width,sH=src.height;src.getContext('2d').drawImage(srcImg,0,0);var pSrc=src.getContext('2d').getImageData(0,0,sW,sH);preProcess(pSrc);var pDst=canvas.getContext('2d').getImageData(0,0,dW,dH);var index=0;if(algo==='lvl'){for(var j=0;j<dH;j++){var y=dY+j;if(y<0||y>=sH){for(var i=0;i<dW;i++,index+=4)setVal(pDst,index,(i+j)%2==0?1:0);continue;}for(var i=0;i<dW;i++){var x=dX+i;if(x<0||x>=sW){setVal(pDst,index,(i+j)%2==0?1:0);index+=4;continue;}var pos=(y*sW+x)*4;setVal(pDst,index,getNear(pSrc.data[pos],pSrc.data[pos+1],pSrc.data[pos+2]));index+=4;}}}else{var errScale=parseFloat(getElm('val_err').value);var aInd=0,bInd=1;var errArr=[new Array(dW),new Array(dW)];for(var i=0;i<dW;i++)errArr[bInd][i]=[0,0,0];for(var j=0;j<dH;j++){var y=dY+j;if(y<0||y>=sH){for(var i=0;i<dW;i++,index+=4)setVal(pDst,index,(i+j)%2==0?1:0);continue;}aInd=((bInd=aInd)+1)&1;for(var i=0;i<dW;i++)errArr[bInd][i]=[0,0,0];for(var i=0;i<dW;i++){var x=dX+i;if(x<0||x>=sW){setVal(pDst,index,(i+j)%2==0?1:0);index+=4;continue;}var pos=(y*sW+x)*4;var old=errArr[aInd][i];var r=pSrc.data[pos]+old[0];var g=pSrc.data[pos+1]+old[1];var b=pSrc.data[pos+2]+old[2];var colVal=curPal[getNear(r,g,b)];pDst.data[index++]=colVal[0];pDst.data[index++]=colVal[1];pDst.data[index++]=colVal[2];pDst.data[index++]=255;r=(r-colVal[0]);g=(g-colVal[1]);b=(b-colVal[2]);if(i==0){errArr[bInd][i]=addVal(errArr[bInd][i],r,g,b,7.0*errScale);errArr[bInd][i+1]=addVal(errArr[bInd][i+1],r,g,b,2.0*errScale);errArr[aInd][i+1]=addVal(errArr[aInd][i+1],r,g,b,7.0*errScale);}else if(i==dW-1){errArr[bInd][i-1]=addVal(errArr[bInd][i-1],r,g,b,7.0*errScale);errArr[bInd][i]=addVal(errArr[bInd][i],r,g,b,9.0*errScale);}else{errArr[bInd][i-1]=addVal(errArr[bInd][i-1],r,g,b,3.0*errScale);errArr[bInd][i]=addVal(errArr[bInd][i],r,g,b,5.0*errScale);errArr[bInd][i+1]=addVal(errArr[bInd][i+1],r,g,b,1.0*errScale);errArr[aInd][i+1]=addVal(errArr[aInd][i+1],r,g,b,7.0*errScale);}}}}canvas.getContext('2d').putImageData(pDst,0,0);drawCropBox();}function buildBMP(){var c=getElm('canvas');var w=c.width,h=c.height;var p=c.getContext('2d').getImageData(0,0,w,h);var row=(w*3+3)&~3;var sz=54+row*h;var buf=new ArrayBuffer(sz);var dv=new DataView(buf);dv.setUint8(0,0x42);dv.setUint8(1,0x4D);dv.setUint32(2,sz,true);dv.setUint32(10,54,true);dv.setUint32(14,40,true);dv.setInt32(18,w,true);dv.setInt32(22,h,true);dv.setUint16(26,1,true);dv.setUint16(28,24,true);dv.setUint32(30,0,true);var view=new Uint8Array(buf,54);var idx=0;for(var y=0;y<h;y++){var line=(h-1-y)*w*4;var off=0;for(var x=0;x<w;x++){var i=line+x*4;view[idx+off+0]=p.data[i+2];view[idx+off+1]=p.data[i+1];view[idx+off+2]=p.data[i];off+=3;}idx+=row;}return buf;}var uploadLoading=null;function createLoadingTip(){var div=document.createElement('div');div.id="upload-tip";div.style.position='fixed';div.style.top='50%';div.style.left='50%';div.style.transform='translate(-50%,-50%)';div.style.background='rgba(0,0,0,0.75)';div.style.color='#fff';div.style.padding='24px 40px';div.style.borderRadius='8px';div.style.zIndex='9999';div.style.fontSize='16px';div.textContent='正在上传，请耐心等待...';document.body.appendChild(div);return div;}function removeLoadingTip(){if(uploadLoading){document.body.removeChild(uploadLoading);uploadLoading=null;}}function uploadImage(){if(!curAlgo||!getElm('canvas')){alert('请先选择图片并执行一次转换');return;}var sendBtn=getElm('Send_Button');uploadLoading=createLoadingTip();sendBtn.disabled=true;var xhr=new XMLHttpRequest();xhr.open('POST','/dataUP');xhr.setRequestHeader('Content-Type','image/bmp');xhr.upload.onprogress=function(ev){if(!ev.lengthComputable)return;var pct=(ev.loaded/ev.total*100).toFixed(1);uploadLoading.textContent='正在上传 '+pct+'%';};xhr.onload=function(){sendBtn.disabled=false;removeLoadingTip();if(xhr.status>=200&&xhr.status<300)alert('上传成功：'+xhr.responseText);else alert('上传失败！服务器返回错误');};xhr.onerror=function(){sendBtn.disabled=false;removeLoadingTip();console.error('网络异常');alert('上传失败！网络错误');};xhr.timeout=15000;xhr.ontimeout=function(){sendBtn.disabled=false;removeLoadingTip();alert('上传超时，请检查Wi‑Fi连接');};xhr.send(new Blob([buildBMP()],{type:'image/bmp'}));}function showSliders(algo){getElm('row_mono').style.display=algo==='lvl'?'':'none';getElm('row_err').style.display=algo==='dif'?'':'none';}function syncSlider(id){var range=getElm('nud_'+id);var val=getElm('val_'+id);val.value=range.value;}function syncRange(id){var range=getElm('nud_'+id);var val=getElm('val_'+id);range.value=val.value;}window.onload=function(){var sb=getElm('srcBox');sb.ondragenter=ignoreDrag;sb.ondragover=ignoreDrag;sb.ondrop=drop;getElm('Refresh_Btn').addEventListener('click',function(){dX=Math.max(0,parseInt(getElm('nud_x').value)||0);dY=Math.max(0,parseInt(getElm('nud_y').value)||0);drawCropBox();});['bright','cont','mono','err'].forEach(function(id){getElm('nud_'+id).addEventListener('input',function(){syncSlider(id);var a=curAlgo;if(id==='bright'||id==='cont'){if(a)procImg(a);}else if(id==='mono'){if(a==='lvl')procImg('lvl');}else if(id==='err'){if(a==='dif')procImg('dif');}});getElm('val_'+id).addEventListener('change',function(){syncRange(id);var a=curAlgo;if(id==='bright'||id==='cont'){if(a)procImg(a);}else if(id==='mono'){if(a==='lvl')procImg('lvl');}else if(id==='err'){if(a==='dif')procImg('dif');}});});getElm('Input_Button').addEventListener('click',function(){getElm('File_Input').click();});getElm('File_Input').addEventListener('change',function(e){processFiles(e.target.files);});getElm('Lvl_Button').addEventListener('click',function(){showSliders('lvl');procImg('lvl');});getElm('Dif_Button').addEventListener('click',function(){showSliders('dif');procImg('dif');});getElm('Send_Button').addEventListener('click',uploadImage);};