#=====================================================================
#                屏幕图片轮播配置文件 config.txt
# 使用重要提醒：
# 1. 以 # 开头的是注释说明，设备不会读取这部分内容；不要在参数同一行后面写注释
# 2. 配置项等号‘=’两边不能加空格
# 3. 文件路径、图片名字区分大小写，路径结尾不加斜杠 /
# 4. 硬件存储空间有限，不支持两层及以上层数的嵌套子文件夹，例如：不支持 folder1/folder2/test.bmp
# 5. 本文件名和存放路径不要修改，必须叫 config.txt
# 6. 修改保存文件后，需要重启设备，新配置才会生效
#=====================================================================

# 【轮播间隔时间】单位：秒；允许范围：300 ~ 86400（5分钟 ~ 24小时）
loop_interval="180"

# 【是否循环当前文件夹图片】1=开启循环，0=关闭循环
# 关闭循环：播完当前文件夹所有图片后，自动切换到下一个文件夹继续播放
loop_subfolder="0"

# 【指定开机最先播放的图片】若双引号空白就自动读取文件夹里第一张可显示图片
# 示例1：start_file_name="demo.bmp"
# 示例2：start_file_name="floder/demo.bmp"
start_file_name=""