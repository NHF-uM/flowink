sockets（网络基础，必须最先理解）
wifi （WiFi 连接基础）
dhcpv4_client（连上 WiFi 自动获取 IP）
dns_resolve（解析域名）
mqtt_publisher（物联网最常用协议，重点学习）
secure_mqtt（加密版本，实际产品必用）
mdns_responder（局域网发现设备，调试很方便）

关键概念极简总结（备忘录）
TCP：可靠通讯，快递签收确认，丢包重传（微信聊天、网页）
UDP：只管发，不管收不收得到，速度快（视频、音频）
MQTT：物联网专用轻量消息协议，“发布 / 订阅模型”
Socket：所有网络通讯的底层通用接口

## Access Point - AP

单片机自己发射 WiFi 热点，外部设备（手机）直接连接单片机的热点


## Station - STA

单片机连接家里 / 路由器的 WiFi，单片机变成局域网里一台设备

> 当单片机处于 AP 模式的时候，外部设备就可以叫做 station



很多 WiFi 单片机项目搭配这样的组合：
- TCP：正式通道，上传图片
- UDP：局域网设备发现（广播）

例子：
手机 APP 不知道单片机 AP 的 IP 是 192.168.1.1，手机发 UDP 广播 255.255.255.255；单片机监听 UDP，收到广播后回复：我是图片设备，地址192.168.1.1；手机拿到 IP，再去发起 TCP 连接上传图片。


应用层      HTTP/自定义图片协议
  ↓
传输层     ┌─ TCP(SOCK_STREAM)
           └─ UDP(SOCK_DGRAM) 
  ↓
网络层    IPv4 / IPv6
  ↓
链路层    Wi-Fi(AP/STA)

IP 地址：手机号（定位是哪一台设备）
端口号：分机号（区分同一台设备里不同程序）
Socket：成功打通之后的通话线路
协议：沟通语言（双方必须统一）
TCP（可靠、面向连接）
    先拨号建立连接（三次握手）
    你：喂，听得到吗？
    对方：我听得到你！
    你：收到，开始对话。
    消息发送后等待对方应答「收到」；**长时间没应答自动重发**；
    保证消息有序送达；
    聊天结束需要互相打招呼，礼貌挂断（四次挥手）。
UDP（无连接、不可靠）
    不用拨号建立通话，拿起话筒直接喊话；
    发完立刻结束，不等待确认、不会重传、不保证顺序；
    对方能不能听见、听的顺序对不对，一概不管。

- 为自己设置静态 ip 地址和网关掩码（酒店前台）
- 创建 address pool地址池，将把里面的 ip 分配出去
- 定义分配起始地址，从这个地址开始（地址池起始地址），为不同的设备分配 ip