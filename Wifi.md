- WebSocket：发一条特殊的 HTTP GET 请求，带上升级字段告诉服务端，这条 TCP 连接别断开，规则升级；
  - TCP 连接永久保持不断开，不再需要每次都发送请求；
  - 双向自由收发：手机浏览器可以随时发指令（暂停、切歌），ESP 也能主动推送消息（播放完毕、图片上传成功）；
  - 数据不再遵守 HTTP 请求 / 响应格式，收发二进制、文本都可以，更轻量化。

## Access Point - AP

单片机自己发射 WiFi 热点，外部设备（手机）直接连接单片机的热点


## Station - STA

单片机连接家里 / 路由器的 WiFi，单片机变成局域网里一台设备

> 当单片机处于 AP 模式的时候，外部设备就可以叫做 station



很多 WiFi 单片机项目搭配这样的组合：
- TCP：正式通道，上传图片
- UDP：局域网设备发现（广播）

例子：
手机 APP 不知道单片机 AP 的 IP 是 192.168.1.1，手机发 UDP 广播 255.255.255.255；单片机监听 UDP，收到广播后回复：我是图片设备，地址192.168.1.1；手机拿到 IP，再去发起 TCP 连接上传图片。


【第4层：应用层】 （对应OSI 5+6+7层）
负责业务数据语义：命令、网页、文件、设备交互
├─ 基于TCP：
│  • HTTP / HTTPS(HTTP+TLS)    网页服务
│  • MQTT                     IoT消息
│  • FTP / SFTP               文件传输
│  • Telnet、SSH              远程终端
│  • Modbus TCP               工业协议
├─ 基于UDP：
│  • DHCPv4 / DHCPv6          自动分配IP
│  • DNS                      域名解析
│  • NTP                      网络时间同步
│  • CoAP                     IoT轻量协议
│  • QUIC（HTTP/3底层UDP）
└─ Zephyr对应组件：http_server, mqtt, coap, dhcpv4, net_sockets

        ↓（调用Socket API，数据加上传输层头部）
【第3层：传输层】（OSI第4层）
端到端通信，区分一台设备上多个应用程序（端口号）
├─ TCP（可靠、面向连接）
│   • 特性：握手、重传、流量控制、有序、不丢包
│   • 使用：HTTP、MQTT、SSH
├─ UDP（无连接、轻量、不可靠）
│   • 特性：无握手，开销小；不保证送达
│   • 使用：DHCP、DNS、NTP、音视频
└─ Zephyr：原生网络栈内置TCP/UDP实现

        ↓（增加IP头部，路由寻址）
【第2层：网际层 / 互联网层（OSI第3层）】
跨网段寻址，核心：IP地址
├─ IPv4（32位地址，你当前项目主力）
│   • 配套协议：
│   │   • ICMPv4（ping命令底层）
│   │   • ARP（IP ↔ MAC地址互相解析【极其关键】）
│   │   • DHCPv4（**不在这里！DHCP是应用层UDP协议** 很多人搞错！）
├─ IPv6（128位地址）
│   • ICMPv6、NDP（替代ARP）、DHCPv6
└─ Zephyr：CONFIG_NET_IPV4 / CONFIG_NET_IPV6

        ↓（封装成二层帧，通过硬件介质发送）
【第1层：网络接口层 / 链路层（OSI 1+2层）】
只负责「相邻两台设备」收发数据；**没有IP概念，依靠MAC地址通信**
两大类：有线链路 / 无线链路
├─ 802.3 以太网
│   • MAC帧、PHY芯片
├─ 802.11 WiFi（你ESP32-S3 SoftAP/STA）
│   • WiFi STA（连路由器） / WiFi AP（开热点）
│   • 802.11 无线帧，依靠无线MAC地址通信
├─ USB RNDIS / ECM（USB模拟网卡）
├─ 4G/5G 模组（PPP协议）
├─ CAN、BLE 6LoWPAN（低速物联网链路）
├─ WiFi MAC 工作模式：
│   • STA 模式（无线客户端，连接外部路由器）
│   • AP 模式（无线接入点，手机连我的热点）
│   • AP+STA 共存模式（你当前ESP32-S3工程）
└─ Zephyr：wifi驱动、eth驱动、net_if（统一网络接口抽象）

IP 地址：手机号（定位是哪一台设备）
端口号：分机号（区分同一台设备里不同程序）
Socket：成功打通之后的通话线路
协议：沟通语言（双方必须统一）
TCP（可靠、面向连接）
    先拨号建立连接，需要对方确认；**长时间没应答自动重发**。

UDP（无连接、不可靠）
    不用建立通话，拿起话筒直接喊话；发完立刻结束，不等待确认、不会重传、不保证顺序。

AP 模式：
- 为自己设置静态 ip 地址和网关掩码（酒店前台）
- 创建 address pool地址池，将把里面的 ip 分配出去
- 定义分配起始地址，从这个地址开始（地址池起始地址），为不同的设备分配 ip

# HTTP

永远是客户端主动说话，服务端被动应答，一次对话之后断开 TCP 连接，设备（服务端）没办法主动给手机浏览器发消息

http和https，证书？

HTTP 只管两件事：
监听本机某个端口（默认 80 端口）；接收 TCP 连接，解析 GET/POST 请求、回复数据。
它根本不关心底层网络是怎么连通的：不管网络是：STA 连路由器、ESP 开 AP 热点、USB 网卡、有线网，只要 TCP 通路能打通，HTTP 服务就能正常工作。

HTTP：超文本传输协议，明文传输的基础规范，分为多个版本：HTTP/1.1、HTTP/2、HTTP/3；
HTTPS： HTTP + TLS 加密层，并不是独立的协议版本。

http_service 只是Zephyr设计的软件封装，管理一个 web服务

## 动作方法

GET：单纯拿数据。打开网页、查询设备播放状态、点击播放按钮……
POST：专门用来「提交数据、上传文件、提交表单」。上传图片、提交 WiFi 账号密码……
PUT：
……

## Form

**表单**：以 POST 为例，把你的数据打包成 HTTP POST 规定的格式 multipart/form-data，顺着 HTTP 请求发给服务器

## 端口

判断要不要新开端口
规则 1：只要都是 HTTP 请求，只是功能不同 → 一律共用 80 端口，新增路由即可，不用新开 http_service
  网页、上传、看图、配 WiFi、提交 JSON 配置，全都属于 HTTP 业务，共用 80。
规则 2：协议不同 / 需要网卡隔离 / 安全隔离 → 新开端口 + 新建 HTTP_SERVICE_DEFINE
  HTTP 网页（80） + 私有 TCP 透传协议（8080）→ 必须分端口
  AP 内网网页 (80) + STA 外网 OTA (8080) → 分开端口绑定不同网卡。

❌ 误区：服务器必须是电脑、云主机
✅ 事实：只要运行了监听端口的 HTTP 服务程序，单片机、ESP32、STM32 都能叫做 HTTP 服务器。
你的 ESP32 开启 AP+http_service 之后，本质就是一台小型 Web 服务器。
❌ 误区：HTTP 协议自带「服务器」结构
✅ 事实：HTTP 只是通信协议。服务器的监听、路由分发、并发管理能力，全是 Zephyr / Nginx / ESP‑IDF 这些软件框架额外实现的。
HTTP 协议本身不知道什么是路由、什么并发、什么 404 页面。

## URL

1. 从服务器路由设计角度：一条路由只绑定一个回调，天然只负责一类事情
/index.html 路由只用来返回网页 HTML，不处理上传；
/dataUP 路由只接收 POST 上传图片，不返回页面；
2. 但 HTTP 协议本身没有限制：同一个 URL 完全可以做多件事
依靠请求方法（GET/POST）区分就能实现：
对 /dataUP 发起 GET：ESP 返回当前内存里的图片；
对 /dataUP 发起 POST：ESP 接收上传的图片。
同一个 URL，不同请求方式，执行两套逻辑。
3. 开发规范上，习惯「一个 URL 对应一类功能」
GET 用来获取数据 / 页面；
POST 用来提交、上传数据；
分开之后代码更容易维护、排查问题，并不是 HTTP 强制要求。



HTTP 协议
  只传输 GET/POST + URL 字符串，只管收发报文，不管拿到 URL 之后服务器要干什么动作。
路由规则（Zephyr/http_service、Nginx、ESP-IDF 实现的逻辑）
  人为写死映射关系：URL + 请求方式 → 固定回调函数（固定行为）。



现代网页是拼装结构，不是单独一个完整文件，由 4 类零件拼接而成：
HTML（骨架）
  访问 http://192.168.4.1/index.html，拿到网页的整体结构（输入框、上传按钮、图片预览框这些布局）。
CSS（衣服样式）
  HTML 只负责结构，没有样式、不好看；HTML 内部写了 <link href="/assets/css/style.css">，浏览器解析 HTML 后，会自动发起新 GET 请求去下载 CSS，用来渲染页面样式。
JS（手脚交互能力）
  HTML 里嵌入了 <script src="/assets/js/main.js">，浏览器继续请求 JS 文件，JS 代码负责：监听上传按钮、选中图片、提交上传请求、拉取 ESP 图片预览等交互功能。
图片、字体、图标等资源
  页面里的图标、背景图，也会触发额外 URL 请求。